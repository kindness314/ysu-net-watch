# 更新日志

## v0.3.0-rc.1 — 0.3.0rc1 账号/IP 冻结与安全加固预发布

- 将门户返回的登录来源 IP 冻结、封禁、锁定、限制/黑名单与账号冻结分开分类；
  CAS 页面、服务登录 JSON 以及在线状态消息均使用同一套严格校验。
- 新增退出码 `26`（来源 IP 被门户冻结或封禁）；该类别第一次明确返回后立即停止，
  不进入五次重试，也不触发定时器补偿。账号冻结/锁定仍返回 `23`，设备数上限仍返回 `24`。
- IP 分类要求同时出现 IP/来源地址上下文和明确封禁状态词，普通 IP 地址校验或网络
  故障不会误判为冻结；新增门户、监听、CLI 和定时器离线回归测试。
- 当前改动未使用真实账号或提交认证请求；公开门户无法在无校园网络时提供冻结错误枚举，
  如现场返回新的脱敏文案，可继续补充规则。
- 安全审计未发现源码或 Git 历史中的真实凭据；Bandit 无中高危项，项目声明依赖的
  `pip-audit` 检查无已知漏洞。新增诊断文本的终端控制字符清理、定时状态文件上限、
  凭据 blob 校验和状态查询错误脱敏。
- 完整离线测试共 257 项，2 项按环境条件跳过；预发布 Windows 资产及 SHA-256
  见 GitHub Release `v0.3.0-rc.1`。

以下 fix1～fix16 条目是本次预发布的开发记录，不单独对应公开 Release。

## 未发布 — 0.3.0.dev20260915+fix16 主菜单顺序整理版

- 主菜单按常用程度重排为：校园网监听、默认宽带监听、停止监听、当前账号下线、
  账号管理、常用/定时设置、退出程序。
- 连接/运行控制集中在前半段，账号与配置集中在后半段；现有功能和数据格式不变。
- 新增主菜单顺序回归测试；完整离线测试共 244 项，2 项按条件跳过。
- 已生成候选程序 `release/ysu-net-watch-menu-order-candidate-fix16-20260915.exe`，
  SHA-256 为 `53DB89EEFABA1C01EF0543E6A36DA19CD0A02BE9A6750BCA6375946811DDD3F6`。

## 未发布 — 0.3.0.dev20260915+fix15 账号一次性试验证版

- 新增 `account verify --profile ID`，只执行一轮受控门户认证探测，不启动持续监听、
  不记住最近选择、不按账号表回退，也不踢下线其他设备。
- 当前已有在线会话时默认不读取或提交指定账号凭据，返回退出码 `25`；显式使用
  `--force-switch` 才会先下线当前会话再验证，仍不会触发旧设备下线流程。
- 账号管理的账号编辑菜单中可直接进入试验证；密码拒绝、账号冻结/锁定、设备数上限、
  网络和协议异常继续复用 `23`、`24`、`21`、`30` 分类。
- 新增 PortalClient 不踢旧设备的验证路径和 CLI 回归测试；完整离线测试共 243 项，
  2 项按条件跳过，未使用真实凭据或提交真实认证。
- 已生成候选程序 `release/ysu-net-watch-account-verify-candidate-fix15-20260915.exe`，
  SHA-256 为 `EE454A02ECB2D5DF1DE9D468468DE3B7A9CFEFBEA8336A37DC43065576FB7486`。

## 未发布 — 0.3.0.dev20260915+fix13 账号管理便捷操作版

- 账号管理列表明确显示“修改：账号别名 · 服务”，进入后即可修改别名、凭据、常用状态、
  监听目标或删除；“添加账号”仍在同一栏目内，不需要进入常用/定时设置。
- 账号选择器增加“常用”和“当前监听”标记；新增和修改操作补充成功提示，降低选错账号
  或误以为保存失败的概率。
- 复核主菜单、定时器、夜间保护、下线和监听停止流程；保留 Enter/取消路径和原有数据格式。

## 未发布 — 0.3.0.dev20260915+fix14 夜间保护直接开关版

- “常用与定时设置”直接显示“夜间保护：开/关”，选择该项即可切换；默认仍为开启。
- 恢复时间改为独立的“修改夜间保护恢复时间”项，避免为了开关保护再进入子菜单。
- 保留旧设置文件中 `night_pause_enabled` 的读取和默认开启逻辑；新增开关与迁移回归测试。
- 当前完整离线测试共 237 项，2 项按条件跳过；未使用真实凭据或提交认证请求。
- 已生成候选程序 `release/ysu-net-watch-night-toggle-candidate-fix14-20260915.exe`，
  SHA-256 为 `39010302931D3AA7C54A5C33C850033B09569031EB24FF2DCEFDC4FD7741ECA2`。

## 未发布 — 0.3.0.dev20260915+fix12 账号管理独立栏目版

- 主菜单新增独立的“账号管理”栏目；可直接进入账号档案列表，新增、切换、设为常用、
  修改凭据、改名或删除账号。
- “修改常用/定时设置”现在只保留常用认证模式、定时器和夜间停网设置，减少进入账号
  管理时的层级；原有定时器绑定和账号数据格式不变。
- 更新菜单导航回归测试，并生成 fix12 候选程序；未使用真实凭据或提交认证请求。

## 未发布 — 0.3.0.dev20260915+fix11 认证设备上限与账号顺序回退版

- 将门户明确的认证设备数量/终端数量上限识别为独立的 `account_device_limit` 错误，
  返回退出码 `24`；不踢设备、不进入同账号五次重试，也不在当天补偿时间再次提交。
- 新增可选 `--try-next-account`：仅在退出码 24 时按账号表顺序尝试后续同模式/同运营商
  档案；成功即停止，候选账号也达到上限或遇到其他错误即停止，不循环回退。
- 控制台传入该开关后，手动切换和定时监听均启用顺序回退；界面监听目标随实际候选账号
  更新。默认不开启，环境变量凭据没有账号表回退。
- 新增设备上限分类、退出码、顺序回退和开关默认关闭的离线回归测试；当前完整离线测试
  共 233 项，2 项按条件跳过。
- 已生成本地候选程序 `release/ysu-net-watch-account-fallback-candidate-fix11-20260915.exe`；
  SHA-256 为 `A41286DC1EAB94036CADFCEAE34403D18CF1A495CA69E8554A67D82EE45C56AC`。

## 未发布 — 0.3.0.dev20260915+fix10 下线状态复核版

- 修复门户状态接口先返回离线时直接跳过下线请求的问题；现在会建立只读门户会话，
  通过 `userOnline` 二次确认仍在线后再调用下线接口。
- “未确认在线会话”不再显示成“已经处于下线状态”，明确说明未发送下线请求且
  Wi-Fi 仍保持连接。
- 新增下线误判回归测试；完整离线测试共 226 项：224 项通过，2 项按条件跳过。

## 未发布 — 0.3.0.dev20260915+fix9 退出码界面说明版

- 控制台监听结束、单次登录、下线和 Wi-Fi 失败提示会同时显示数字退出码及中文
  原因；未知退出码也会显示“未知退出状态”，便于定位问题。
- 完整离线测试共 225 项：223 项通过，2 项因当前环境无真实凭据而跳过。

## 未发布 — 0.3.0.dev20260915+fix8 凭据拒绝边界修复候选版

- 增加密码/账号错误的窄匹配，覆盖 CAS 页面和服务登录返回；普通“系统繁忙”、
  “认证失败”等泛化消息不会被误判成密码错误。
- 明确密码被拒绝或账号冻结、锁定时只提交一次，返回专用终止码 23，不进入五次
  认证循环，也不会让当天定时器在补偿时间重复提交相同凭据。
- 协议变化终止码 30 同样不会触发当天失败补偿；暂时性认证失败仍保留最多五次
  的边界，网络异常继续按待恢复处理。
- 完整离线测试共 224 项：222 项通过，2 项因当前环境无真实凭据而跳过；测试期间
  未提交真实认证请求。

- 无凭据实网诊断确认：当前 `iYanDa` 匿名入口的官方链路与安全客户端链路得到相同
  `JSESSIONID`，CAS 页面加密与 execution 字段完整，CAS 前工作流节点已存在；未执行
  CAS 提交，未读取账号密码。
- 纠正此前对 `getCurrentNode` 的误解：该接口只读取流程状态，不负责推进流程。删除
  CAS 前及服务列表前的推测性调用，改为在 `serviceSelection` 后、`serviceLogin` 前
  检查非空节点，缺失时不提交服务登录。
- `serviceLogin` 返回普通错误、`workFlowNode is empty` 或传输结果不确定时，先轮询
  `userOnline`；门户已在线即按成功处理，避免重复提交认证或误踢设备。
- 门户明确拒绝凭据或报告冻结/锁定时仍立即停止且不复核重试；工作流为空且确认仍
  离线时丢弃 Cookie 和会话，只允许一次全新门户会话恢复，第二次仍失败即退出 30。
- 踢设备前先确认当前会话并未在线；工作流会话重试与踢设备共用跨重试恢复状态，
  各自最多一次。

## 未发布 — 0.3.0.dev20260914+fix6 防账号锁定候选版

- CAS 或服务接口明确返回账号/密码拒绝、冻结、锁定、封禁或停用时，第一次响应后
  立即以退出码 20 停止，不再进入五次重试；普通暂时性认证失败仍保留原有上限。
- 账号状态识别只匹配明确措辞，不把笼统的“认证失败”误判为冻结。
- 7 月旧版有真实联通认证成功记录；本候选版及 fix5 尚未完成解冻后的实网认证，
  不将离线回归测试表述为实网可用证明。
- 新增并调整 6 项防锁回归，合计 211 项：209 项通过、2 项按条件跳过；测试
  禁止真实 HTTP，未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260914+fix5 门户状态初始化修复版

- 读取当前门户公开前端模块后确认，首次认证的实际顺序为：进入门户后先调用
  `getCurrentNode` 初始化服务端流程，完成 CAS 后再次读取节点，再获取服务列表并
  调用 `serviceLogin`；上一版把首次节点查询放到了服务列表之后。
- 登录改为严格匹配上述顺序；只有 `serviceLogin` 成功后才读取下一节点，失败响应
  不再被额外的节点请求掩盖。`setService` 仅用于在线后的服务切换，不加入首次登录。
- 节点接口返回对象但缺少非空 `currentNodePath` 时按协议变化停止，不继续提交凭据。
- 新增并调整 5 项流程回归，合计 205 项：203 项通过、2 项按条件跳过；测试禁止
  真实 HTTP，未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260913+fix4 门户工作流修复版

- 远程脱敏日志确认，跳转修复后认证已进入 `serviceLogin`，门户返回 code 400：
  `workFlowNode is empty`；凭据已经通过 CAS，本错误不是账号密码错误。
- 补回门户要求的 `/eportal/workFlow/getCurrentNode` 状态推进：服务选择后、服务
  登录后各调用一次，顺序与参考实现及门户浏览器流程一致。
- 若门户仍返回 `workFlowNode is empty`，按协议变化立即停止，不再重复提交五次。
- 新增 3 项离线回归，合计 203 项：201 项通过、2 项按条件跳过；测试禁止真实
  HTTP，未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260913+fix3 门户重定向兼容版

- 实网只读追踪确认：`auth1.ysu.edu.cn/eportal/redirect.jsp` 当前会把浏览器先
  重定向到同域名的 HTTP `portal/portal-main`，该地址随即以 308 返回同路径
  HTTPS；是否绕过系统代理，链路均相同。
- 对且仅对 `GET http://auth1.ysu.edu.cn:80/portal/portal-main` 在本地升级为
  HTTPS，保留查询参数但不发送明文 HTTP 请求；其他降级、路径、主机和端口仍拒绝。
- `unsafe_redirect` 改为立即以退出码 30 停止，不再无意义地累计五次认证失败；
  错误中只显示脱敏后的协议、主机、端口和路径，不记录查询参数。
- 新增 3 项离线回归，合计 200 项：198 项通过、2 项按条件跳过；测试禁止真实
  HTTP，未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260912+fix2 定时幂等版

- 新增独立的定时状态账本，任务领取、主任务失败和补偿领取日期会原子持久化；
  同一天退出或重启程序不再重复补执行、强制下线和认证。任务配置或凭据版本变化
  使用新指纹，旧结果不会串到新目标。
- 状态账本只保存 SHA-256 任务指纹和日期，不保存账号、密码或凭据；文件损坏时
  安全停止定时调度，不以空状态重复认证。日期关系不一致也会拒绝加载。
- schema 2 对每个定时器的开关、时间、日期、模式、运营商、补偿时间及字段类型
  进行严格校验；旧版无 schema 设置继续按兼容规则迁移。
- 补偿时间必须晚于同日主任务；交互界面会直接提示，保存层和加载层再次校验。
- 冲突任务只在较高优先级任务已被接受后才记为跳过。优先任务账号缺失或启动前
  失败时，其余到期任务仍可在后续调度周期执行。
- 新增 9 项离线回归，合计 197 项：195 项通过、2 项按条件跳过；测试禁止真实
  HTTP，未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260912+fix1 可靠性修复版

- 定时器不再只匹配精确分钟；程序启动、从休眠恢复或调度线程暂时阻塞后会补执行
  当天错过的任务。已晚于补偿时间时只补一次主任务，避免主任务与补偿连续运行。
- 定时任务遇到 Windows 无法确认 Wi-Fi 状态时按失败记录并保留补偿资格；明确连接
  热点或其他 Wi-Fi 时仍安全跳过，不切换网络、不提交凭据。
- 停止信号在 Wi-Fi 初始化前生效，连接命令和等待阶段会检查取消状态；日志文件临时
  不可写时仅提示一次，监听和认证状态机继续运行。
- 同一监听被多个定时器复用时分别保留任务归属，监听结束会将结果回写给全部关联任务，
  避免较早定时器的失败补偿丢失。
- 严格拒绝类型错误的 `schema_version` 和 schema 2 核心字段，避免损坏设置被静默当作
  旧配置加载，或因列表/字典等异常值抛出未处理的 `TypeError`。
- 新增 12 项离线回归，合计 188 项：186 项通过、2 项按条件跳过；测试禁止真实 HTTP，
  未进行校园网认证、Wi-Fi 切换或真实凭据读写。

## 未发布 — 0.3.0.dev20260912 定时跟随版

- 默认定时器 1 跟随最近确认的手动账号和运营商；主任务和失败补偿使用同一目标。
  手动监听、常用设置及命令行档案选择都会记住目标，重启后仍有效。
- 定时器 2～10 继续固定绑定；自动任务、取消和停止监听不改最近选择；
  时间、日期、开关和补偿时间保持不变。切换账号使旧账号的失败记录失效。
- 旧配置首次按当前常用账号初始化跟随目标；缺失账号不回退到其他账号。
  多账号旧设置首次保存前备份为 settings.pre-timer-follow.json。
- 菜单区分跟随与固定绑定，删除最近选择的账号时停用引用并清空选择。
- 新增 18 项离线回归，合计 176 项：174 项通过、2 项按条件跳过。

## 未发布 — 0.3.0.dev20260910+fix1 结构修复版

- 重连期间保留明确认证失败次数；网络超时不计次，也不再清空此前的失败。
- 同一轮恢复最多请求一次旧设备下线；请求超时结果不确定时也不重复踢设备。
- 定时调度增加异常恢复和独立健康状态显示；Wi-Fi 查询失败记录主任务失败，
  继续检查后续定时任务及已配置的失败补偿。错误日志不可写时仍保留菜单提示。
- 定时任务绑定账号、凭据版本、配置代次与日期；编辑/改绑使旧任务结果失效，
  账号改名不影响补偿。过期线程结果不能触发新账号的补偿。
- 新增 22 项离线回归，合计 158 项：156 项通过、2 项按条件跳过。
- 尚未访问到远程电脑的实际日志，不将本次结构修复视为其退出码 20 的现场诊断。

## 未发布 — 0.3.0.dev20260910 多账号预览版

- 新增最多 20 个账号档案，支持添加、改名、更新账密、设置常用、删除及切换监听。
- Windows 凭据按档案 UUID 和版本隔离；旧宽带兼容档案采用首次更新时独立保存的方式迁移。
- 定时器绑定账号 ID；删除引用保护，修改常用账号不改变定时器。
- 登录、下线、凭据修改和账号操作纳入单实例保护，旧线程迟到状态不会覆盖新监听。
- 周一至周四 23:30 到次日定时器 1 的时间启用停网保护；暂停期间不消耗认证失败次数。
- 夜间保护可关闭；结束时间独立于定时器 1 的开关。早晨 Wi-Fi 暂时不可用时继续重试，
  热点保持不动；在运行的监听会关联早晨主定时器，以保留认证失败后的补偿。
- 单次操作的夜间保护退出码为 22；新增 `--version`。
- 设置写入采用唯一临时文件，旧配置首次保存时备份；损坏配置禁止回退到旧账号认证。

## 未发布 — 2026-09-09 本地修复版

- 分离门户认证和外网验证，门户在线但探测不确定时继续监听，避免误报退出码 20。
- 上线/下线增加有上限的状态复核；同一登录流程只执行一次旧设备下线。
- 请求超时与网络异常单独归类为待恢复；单次操作使用退出码 21。
- 认证重试重新读取 Windows 凭据，监听中的请求和补偿等待响应停止及 Wi-Fi 变化。
- 保留运营商业务错误码与消息，终止日志保留最后失败原因；复用既有会话不再声称新账号认证成功。
- HTTP 流式响应中途断开时记为网络不确定并关闭响应。
- 多账号切换方案写入 PLAN.md，本版尚未实现多账号管理。

## 0.2.1 - 2026-07-31

- 修复已连接 `iYanDa` 时重复执行 Windows Wi-Fi 连接，避免跨设备切换时误报退出码 40。
- 保留 `netsh` 的退出码和 Windows 原始错误，方便定位网卡、权限和 SSID 问题。
- 扩展常驻控制台的错误摘要，避免 Wi-Fi 故障只显示省略号。
- 修正发布版手动验证说明中的默认日志路径。

## 0.2.0

- 首次公开发布。
